# Contributing Guide

landit-admin 개발 그라운드 룰. landit-fe와 같은 규칙을 쓰되, 앱이 하나라 scope가 없고 보안 리뷰 항목이 추가됐다. 아키텍처 레벨 결정은 `docs/`에, 코드 레벨 논의는 PR에 정리한다.

## 그라운드 룰

- 개발 내용은 사람이 직접 검토한다.
- 문서화 내용은 사람이 한 번 검토한다.
- 화면 스펙은 `docs/screens/*.md`가 기준이다. 스펙과 다르게 구현해야 하면 스펙 PR을 먼저 낸다.

## 코드 컨벤션

[Google Style Guide](https://google.github.io/styleguide/)를 참고한다.

- 네이밍은 길어도 괜찮으니 직관적으로 작성한다.
- 하나의 메서드는 하나의 책임과 기능만 갖는다. (최대 20줄, 파라미터 최대 3~4개)
- 메서드 설명은 `/**` (JSDoc), 메서드 내부 주석은 `//`
- 새 소스 파일 첫 줄에 역할을 한 줄 한국어 주석으로 적는다 (`'use client'` 등 지시문 바로 아래).

### 포맷팅과 import 순서

Prettier가 저장/커밋 시 자동으로 맞춰주므로 수동으로 신경 쓸 필요 없다. 규칙은 `.prettierrc`에 정의되어 있다.

- 따옴표는 single quote, 후행 콤마 사용
- import 순서 (그룹 사이 빈 줄)
  1. `react`
  2. 외부 패키지
  3. `@/*` (내부 alias)
  4. 상대 경로 (`./`, `../`)
- Tailwind 클래스는 공식 권장 순서로 자동 정렬 (`prettier-plugin-tailwindcss`)
- 전체 포맷은 `pnpm format`

## 브랜치 전략

| 브랜치            | 용도                                         |
| ----------------- | -------------------------------------------- |
| `main`            | 기준 브랜치. 제품을 배포하는 브랜치          |
| `develop`         | 개발 브랜치. 각자 작업한 기능을 여기로 Merge |
| `feat/{이슈번호}` | 단위 기능 개발. 완료되면 `develop`에 Merge   |
| `release`         | `main`으로 보내기 전 QA(품질검사)용 브랜치   |
| `hotfix`          | `main` 배포 후 발생한 버그 긴급 수정         |

- 브랜치 이름에 노션 이슈 번호를 적는다. 예시: `feat/LAN-10`
- `main`, `develop`에 직접 커밋하지 않는다. 초기 설정도 `develop`으로 PR을 올린다.

## 커밋 컨벤션

형식: `{type}: 커밋 메시지`

- **scope 없음** — 앱이 하나다. (landit-fe의 `(web)`·`(mobile)`에 해당하는 구분이 없다)
- **제목**: 한국어 서술형 한 문장 — "~한다", "~로 정리한다", "~~를 뺀다". 명사 나열("~~ 추가")보다 문장이 우선.
- **본문**: "왜"를 산문으로. 무엇을 했는지는 diff가 말하니 이유·배경·트레이드오프를 쓴다.
- 커밋은 1개 30줄 이내를 권장한다.

| Tag      | 설명                                                               |
| -------- | ------------------------------------------------------------------ |
| feat     | 새로운 기능 추가                                                   |
| fix      | 버그 수정                                                          |
| design   | 사용자에게 보이는 UI 시각적 변경 (레이아웃, 색상, 컴포넌트 디자인) |
| style    | 코드 포맷 변경, 세미콜론 누락 등 동작에 영향 없는 코드 정리        |
| refactor | 동작은 그대로, 코드 구조/가독성 개선                               |
| type     | 타입 정의 수정                                                     |
| docs     | 문서 수정                                                          |
| comment  | 주석 추가 및 변경                                                  |
| chore    | 빌드, 패키지 매니저, 환경 설정 등 개발 환경 관련                   |
| lint     | ESLint 설정 수정 및 린트 에러 수정                                 |
| deploy   | 빌드 및 배포 작업                                                  |
| test     | 테스트 코드 추가 및 수정                                           |
| rename   | 파일 또는 폴더명 변경                                              |
| remove   | 파일 삭제만 한 경우                                                |
| security | 보안 헤더·인증·프록시·의존성 취약점 등 보안 관련 변경              |

예시.

```
feat: 피드백 목록의 유형·상태 필터를 쿼리스트링에 둔다
fix: 일괄 답장 확인창의 선택 건수가 0으로 보이던 것을 고친다
design: 사이드바 서버 카드 간격을 Figma 셸 스펙에 맞춘다
security: 프록시의 변경 요청에 same-origin 검사를 더한다
docs: 공지·업데이트 스펙에 발행 취소 흐름을 적는다
chore: 스웨거에서 API 타입을 생성하는 스크립트를 둔다
!HOTFIX: 프로덕션에서 로그아웃 후 쿠키가 남는 문제를 고친다
```

## PR 컨벤션

[뱅크샐러드 코드 리뷰 문화](https://blog.banksalad.com/tech/banksalad-code-review-culture/)를 참고한다.

- PR 제목은 이슈 제목 그대로 작성한다.
- 필요하다면 부연 설명을 위해 본인 PR에 셀프 코멘트를 달아준다.
- PR 코멘트는 '~~바꿔주세요', '~~한 번 고려해주세요' 형태로 작성한다.
- PR 리뷰는 모아뒀다가 오프라인 회의 시 진행한다.
- '코드 추가' 변경 기준 500줄 이하를 권장한다. 기계 생성 파일(shadcn `ui/*`, `schema.d.ts`, lockfile)은 예외로 두되 PR 설명에 "생성 파일, 리뷰 대상 아님"이라 적는다.
- PR은 5~10분 정도로 리뷰 가능한 볼륨으로 작성한다.

### PR 쪼개기

PR = 이슈 1개 = 한 문장으로 설명되는 변경. 화면 하나가 500줄을 넘으면 이 순서로 가른다.

1. 스펙 문서 (`docs/screens/*.md`) — 코드 없음
2. `api` + `model` + 테스트 — 화면 없이 로직만
3. `ui` + `page` — 스펙대로 그리기
4. 상세·모달·일괄 액션 같은 부가 흐름은 각각 별도 PR

세팅 단계의 전체 PR 순서는 [AGENTS.md](AGENTS.md)의 "PR 쪼개기" 표에 있다.

### 보안 리뷰 포인트

아래를 건드리는 PR은 리뷰어가 반드시 확인한다. 항목은 [docs/security.md](docs/security.md)와 같다.

- `app/api/**` `proxy.ts` `shared/auth/**` — 토큰이 쿠키 밖(응답 body·로그·URL·클라이언트 상태)으로 새지 않는가. 화이트리스트·CSRF 검사·no-store가 유지되는가
- `next.config.ts` — 헤더·CSP가 약해지지 않았는가. 새 외부 도메인이 문서화됐는가
- `dangerouslySetInnerHTML` · `NEXT_PUBLIC_` 새 변수 · 새 의존성 · `.npmrc`/`pnpm-workspace.yaml` 변경 — 이유가 PR에 있는가
- 되돌릴 수 없는 작업 — 확인창이 있고 대상이 표시되는가

## 이슈 관리

- 노션에 이슈를 정리한다. 각 이슈에는 고유 번호가 존재한다. (예: LAN-10)
- 이슈는 제3자가 봐도 이해 가능하도록 최대한 자세하게 작성한다.
- 이슈 = PR 단위. 개발하고 merge 되었으면 수동으로 노션 체크박스를 닫는다.
