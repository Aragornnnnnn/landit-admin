// 자리표시자 — 에디터 PR(docs/screens/letters.md "새 편지 / 편집")에서 채운다
import { ComingSoon } from '../../_ui/ComingSoon';

export default function Page() {
  return <ComingSoon spec="docs/screens/letters.md — 편지 편집" />;
}
